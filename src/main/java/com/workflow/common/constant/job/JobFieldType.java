package com.workflow.common.constant.job;

public enum JobFieldType {
    TEXT,
    NUMBER,
    DATE,
    BOOLEAN,
    DROPDOWN,
    JSON,       // Arbitrary JSON object
    REFERENCE   // Foreign key / object reference
}
