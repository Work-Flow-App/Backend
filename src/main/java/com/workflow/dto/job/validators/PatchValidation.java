package com.workflow.dto.job.validators;

/**
 * Validation group marker interface for PATCH requests (partial updates).
 * All fields are optional in PATCH operations.
 */
public interface PatchValidation {}
