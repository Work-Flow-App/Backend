package com.workflow.common.constant.form;

public enum FormRoleTarget {
    COMPANY, // Only the company can fill this field
    WORKER, // Only the worker can fill this field
    BOTH // Both can edit
}